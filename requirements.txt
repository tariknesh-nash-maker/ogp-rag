# Use CPU wheels for torch to keep the build small
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.48.0
pandas==2.3.1
chromadb==1.0.16
sentence-transformers==5.1.0
openai==1.99.6     # only if you’ll use GPT synthesis; else remove

# Let pip resolve a compatible torch cpu build automatically via extra-index
# (If the host fails to resolve, pin an explicit CPU wheel, e.g. torch==2.5.1+cpu)
